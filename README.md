# 🌦️ Proyecto Meteo -- Instalación y Ejecución

Este proyecto está compuesto por dos partes:

-   **Backend** → Carpeta `server`
-   **Frontend** → Carpeta `meteo-client` (Angular 20)

------------------------------------------------------------------------

## ✅ Requisitos previos

Antes de instalar cualquier dependencia, **debes tener instalado Node.js
versión 20 o superior** en tu ordenador.

Puedes verificar tu versión con:

``` sh
node -v
```

------------------------------------------------------------------------

# 🚀 Backend (Server)

### 1️⃣ Entrar en la carpeta del servidor

``` sh
cd ./server
```

### 2️⃣ Instalar dependencias

``` sh
npm install
```

### 3️⃣ Ejecutar el servidor en modo desarrollo

``` sh
npm run dev
```

El backend se ejecutará automáticamente en el **puerto 4000**.

------------------------------------------------------------------------

# 💻 Frontend (Angular 20)

El frontend fue desarrollado con **Angular 20**, por lo que necesitas
tener instalada esta versión del CLI.

### Verificar si tienes Angular 20 instalado

``` sh
ng version
```

Si no lo tienes, instálalo con:

``` sh
npm install -g @angular/cli
```

------------------------------------------------------------------------

### 1️⃣ Entrar en la carpeta del cliente

``` sh
cd ./meteo-client
```

### 2️⃣ Instalar dependencias

``` sh
npm install
```

### 3️⃣ Ejecutar el proyecto Angular

``` sh
ng serve
```

El frontend normalmente correrá en:\
👉 `http://localhost:4200/`

------------------------------------------------------------------------

# 🎉 ¡Listo!

Ahora deberías tener **backend y frontend** ejecutándose correctamente.
